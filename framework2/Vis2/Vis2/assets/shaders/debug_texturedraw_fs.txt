#version 330 core
out vec4 FragColor;

in vec2 TexCoords;

//uniform sampler3D depthMap;
uniform sampler2DArray dmmm;
uniform sampler2DArray depthMap2;
uniform sampler2D tex3;


uniform float near_plane;
uniform float far_plane;

uniform int glLayer;

void main()
{  
     vec4 res = texture(depthMap2, vec3(TexCoords,glLayer));// + texture(depthMap2, vec3(TexCoords,1));
     if (length(res.xyz) < 0.001)
        discard;

     //vec4 res = vec4(texture(tex3, TexCoords).xyz,1);

     FragColor = res;
}
